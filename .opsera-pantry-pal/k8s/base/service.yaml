apiVersion: v1
kind: Service
metadata:
  name: pantry-pal
  # NO namespace field in base (INV-003)
  labels:
    app: pantry-pal
    tenant: opsera
spec:
  selector:
    app: pantry-pal
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
